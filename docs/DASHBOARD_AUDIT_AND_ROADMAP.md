# Dashboard module — end-to-end audit & multi-session roadmap

Owner: Raghotham · Started 2026-09-11 · Status: **A-E all complete (A 7/7, B 5/5, C 5/5, D 4/4, E 4/4)** — F/G/H remain

This is the living source of truth for the dashboard overhaul. Each session:
pick the next unchecked item(s) in the current phase, ship, tick the box, add a
dated note. Phases A–B are the visible UX wins and run first; C–E build the
Company→Project hierarchy; F–H are heavier R&D tracks.

Related existing docs: `DESIGN.md` (design rules), `TODO.md` (§ "Enterprise
readiness", § "Auth / multi-tenancy" — this roadmap supersedes those two
sections), `docs/DATA_LICENSING.md`, `docs/TARGET_CUSTOMER.md`.

---

## 1. What we have today

**13 workspace screens** (`workspace/src/pages/`), routed flat in `App.tsx`:
Dashboard, MapWorkspace, Projects, ProjectWizard, SavedLocations, CustomerData
("Store Data"), Forecast, Connections, Reports, Activity, Credits, Billing,
ApiKeys.

**Data model** (`paisamap-etl/etl/_auth_db.py`): everything is `user_id`-scoped.
`projects` is one flat table carrying *both* company-ish fields (`website_url`,
`business_type`, analytics consent, and OAuth connections + store data are keyed
by `project_id`) *and* project fields (`signals`, `target_pincodes`,
`total_investment`, `catchment_km`, `gross_margin_pct`…).

**`organizations` + `org_members(role)` + `users.org_id` + `projects.org_id`
tables already exist** — created months ago, **zero blueprint endpoints, zero
UI, nothing populates them.** A dormant scaffold.

**Synthesis engine**: `_signals_data` loads ~30 signal columns from
`ppi_ml_refined.csv`; `intelligence.py` / `expansion.py` / `_forecast_model.py`
/ the map consume subsets. GA4/GSC connect via `analytics_connections.py`
(tokens encrypted at rest via `_token_crypto`).

Forecast just got a full visual rebuild (`chartTheme.ts`, new card system) —
**that is the reference bar; every other screen is well below it.**

---

## 2. Audit findings

### 2.1 Navigation & workflow — "the journey is fragmented"

| # | Finding | Heuristic |
|---|---|---|
| N1 | **No global project context.** Forecast, Connections, Store Data, Map each have their *own* independent project `<select>`. Switch project on Forecast → open Map → it resets to `projects[0]`. Only `mapSession` (localStorage) persists a project, and only the Dashboard reads it. | H3, H6 |
| N2 | **No "return to where I was."** Mid-forecast → click "upload store data to calibrate" → land on `/customer-data` with no path back to the forecast you were building. Same wizard→connections, dashboard→map→forecast. Deep-links (`?project_id=`) exist but are applied inconsistently. | H3 |
| N3 | **No breadcrumbs / no location indicator.** You can't tell which project (or, later, which company) you're operating on without opening a dropdown. | H1, H6 |
| N4 | **No project detail page.** `Projects` is a flat list with click-to-expand inline edit. There is nowhere that shows "this project: its map, its saved locations, its last forecast, its reports, its connections". | H6, H8 |
| N5 | "Credits" and "Billing" are two separate nav items that are both "money". "Store Data" (nav) vs `/customer-data` (route) vs "customer locations" (API) — three names for one thing. | H2, H4 |
| N6 | 12 flat nav items, no grouping. Will not scale to Company settings + Members + Demand + … | H8 |

### 2.2 Null / edge / variation states — "when the filter is zero the whole card goes off"

| # | Finding |
|---|---|
| E1 | **Filter → 0 results destroys card chrome.** Forecast `HotspotBubbles` / `SignalRevenuePanel` return a bare `<p>` losing the filter controls + header context. `LocationFactorTable` / `LocationCompareRadar` return `null` → a card with a heading and an empty body. Charts do `if (points.length < 2) return null` in ~6 places. |
| E2 | **Loading = plain `"Loading…"` text**, no skeletons → hard layout jump when data lands (the "UI jump" complaint). |
| E3 | **Card height varies with optional fields.** "Which locations yield well" rows grow/shrink with `overlap −X%`, `revenue capped`, `biz-meta` presence → ragged list (the screenshot). |
| E4 | Whole panels vanish silently: no `nudge` → card gone; `investment_split` empty → different card; `factorSites < 2` → compare radar card absent, no explanation. |
| E5 | Unhandled promise rejections: `Projects.onDelete`, several `.then()` chains with no `.catch()`. |

### 2.3 Consistency & standards

| # | Finding | Heuristic |
|---|---|---|
| C1 | Every list page re-implements layout. `Projects` = `.project-list` + inline-edit; `SavedLocations`/`Reports`/`Forecast` = `.list`; row actions, spacing, meta all differ. | H4 |
| C2 | Destructive actions fire on one click, no confirm, no undo — `Projects` "Delete", `SavedLocations` "Remove". Irreversible (CASCADE). | H5 |
| C3 | Button labels inconsistent ("Delete" vs "Remove"), edit is inline-expand here / navigate-to-wizard there. | H4 |
| C4 | Error display: red `<p>` lines, inconsistent placement; some paths uncaught. | H9 |
| C5 | Project selectors are bare name-only `<select>` — no "12 locations · forecast 3 days ago" context. | H6 |

### 2.4 Visual polish — "most screens are just bland"

- **No per-project map thumbnails** — the Dashboard hero has one generic hand-drawn SVG shared by everyone.
- No sparklines / micro-charts / stat chips in list rows. Rows are text-only.
- Icons only in the sidenav. List rows, chips, section headers have none.
- `.pill` chips are flat, no icons.
- Uppercase-mono kickers everywhere; heavy, dated.
- Forecast's new `chartTheme` + card system is not propagated anywhere else.

### 2.5 Hierarchy gap — Company → Project

The user's model:

```
Account
 └─ Company  (website, analytics connections, store data, USERS + roles)
     └─ Project  (signals, keywords, goals, investments, campaigns,
                  target audience, avg price)  ← forecast runs per project
```

Today: `Account (user) → Project` only. `website_url`, OAuth connections,
store data all sit on the *project* and would need to move to the *company*.
`organizations`/`org_members` are the right tables but completely unwired.

### 2.6 Missing analytics — demand intelligence

Nothing exists for: **existing demand trend**, **demand creation opportunity**,
**demand gen gap**, **demand forecast**. Needs a signal-history store (we have
enrichment cadence but not per-pincode value history over time), keyword/search
volume aggregation (GSC connects but only shows in the Connections page), and a
demand-side model alongside `_forecast_model`.

### 2.7 Signal synthesis — are we "encashing on all signals"?

No coverage map of signal → customer-visible output. Known dead weight:
`cropping_intensity_pct` surfaces only in one SWOT row; **GSC keyword data and
GA4 geography connect but never feed forecast / suitability / demand** — they're
display-only. No feedback loop: a customer's uploaded store revenue calibrates
*their own* capture rate but never enriches the shared signal set. The user
wants (a) a bidirectional coverage audit, (b) anonymised re-synthesis of
customer analytics + store performance into our enrichment, (c) possibly
scheduled AI/ML "self-intelligence" jobs.

### 2.8 Security & data protection

| # | Finding |
|---|---|
| S1 | **Customer store data (`customer_locations`: address, revenue, rent, capex) is plaintext at rest.** Revenue/rent are commercially sensitive. OAuth tokens *are* encrypted (`_token_crypto`) — extend that pattern. |
| S2 | Cross-account isolation looks sound (`get_project(id, user_id)` gates the joins) but there is **no automated test** proving every endpoint/table is isolated. |
| S3 | No data-access audit log (the `activity_log` is user-facing actions only). |
| S4 | Anonymised re-synthesis (2.7c) needs a consent + k-anonymity framework before any customer data feeds the shared set. `analytics_consent_at` exists for GA4 only. |
| S5 | Rate limiting exists only on API-key routes. Secrets rotation / pen-test checklist per `TODO.md` § "Security hardening" still open. |

---

## 3. Roadmap

Ordering: **A → B first** (visible wins, no schema risk). **C → D → E** is the
hierarchy backbone (one migration, do it once, carefully). **F, G, H** are
parallel R&D tracks that can start once C lands.

### Phase A — Dashboard UX foundation  ·  status: **complete (7/7)**

- [x] **A1** Shared primitives: `<DataList>` + `<DataRow>` (`workspace/src/components/DataList.tsx` — title / subtitle / chips / trailing / footer / expanded slots), `<StatChip>` (`StatChip.tsx`), `<MicroBar>` + `<Sparkline>` (`MicroViz.tsx`, reuse `chartTheme.ts` colours/stroke weights). `.project-list` and `.list` CSS fully retired in favour of `.data-list`/`.data-row`; migrated all 9 call sites (Projects, SavedLocations, Reports ×2, ApiKeys ×2, Activity, Billing, Credits, CustomerData ×5, Forecast). `<DataCard>` skipped — `.card` already covers that role, no duplicate needed.
- [x] **A2** `<AsyncBoundary>` (`AsyncBoundary.tsx`) + `<RowSkeleton>`/`<CardSkeleton>` (`Skeleton.tsx`): resolves loading (shimmer skeleton, kills the E2 jump) → error (`.async-error` banner + retry button) → empty (keeps caller's header/filters mounted, fixing E1) → children, in that order. Wired into every top-level list/project gate that used to be a bare `"Loading…"` string with an unhandled `.then()`: Projects, SavedLocations, Reports, ApiKeys, Activity, Billing, Credits, Connections, Forecast, and CustomerData's three gates (projects/locations/drivers). Each now has a real error state + retry instead of hanging forever on a failed fetch (partial E5). Deliberately left alone: CustomerData's `pastUploads` (never had a loading gate, just `.catch(()=>{})` added) and Forecast's chart-level `return null` guards (~6 spots, A6/A2-followup scope, not a page-level async gate) — both are real remaining E1/E4 items but out of this session's scope.
- [x] **A3** `<WorkspaceProvider>` (`lib/workspace.tsx`) — one shared project list + active-project id, persisted to `localStorage` (`pm.workspace.v1`), loaded once instead of per-page. New `<ProjectSwitcher>` in a new persistent top bar (`App.tsx`'s `.topbar`, above `main.content`, hidden on the `/map` bleed route) — the one place a project is now chosen from. `useSyncProjectFromUrl()` mounted once inside the provider: a `?project_id=` deep link still wins on arrival and writes into shared context (N2). Migrated the three pages N1 called out as each keeping an independent `<select>` — Forecast, Connections, CustomerData — to read `useWorkspace()` and dropped their own selects/fetches/local project state entirely; each page's per-project side effects (Forecast's prev-curve/budget reload, CustomerData's upload-reset, Connections' consent/connections reload) now key off the shared `activeProjectId` via `useEffect` instead of an `onChange` handler. Company-level context (the other half of A3's original scope) deferred to Phase C — no `organizations` plumbing exists yet to switch between. Not done: Dashboard's separate `mapSession` (last map pincode) and the map iframe's own state are a different concern (per-viewer map resume, not project selection) and were left alone.
- [x] **A4** `<ReturnToLink>`/`<ReturnBanner>` (`components/ReturnTo.tsx`): a cross-page CTA captures its current location as `return_to`/`return_label` query params; the destination renders a "← Back to {label}" banner reading them back (N2). Wired the exact cases N2 named — Forecast's "Upload store data" CTA (`ForecastPreview.tsx`) and the wizard's "Manage"/"Connect"/"Upload" cards (`ProjectWizard.tsx`) — into Store Data and Connections, which both render `<ReturnBanner>`. Dashboard's resume hero (`map-hero` block) turned out to already cover the "Resume rail driven by real recent context" bullet — last map session, saved-location count, "Resume forecast"/"Resume on the map" CTAs were already there; only cleanup done was pointing its project list at `useWorkspace()` instead of its own `listProjects()` call, so it shares A3's context instead of duplicating the fetch. Breadcrumb bar (N3) deliberately **not** built — A3's persistent top-bar switcher already shows the active project without opening anything, which was N3's actual complaint; a second breadcrumb saying the same thing would be redundant chrome, not a fix.
- [x] **A5** `usePendingDelete()` (`lib/undo.ts`) + `<UndoToastStack>` (`components/UndoToast.tsx`): a delete hides the row immediately and defers the actual API call 5s, so a misclick is free to reverse (no separate blocking `confirm()` needed — the undo window **is** the confirmation, and it's non-blocking unlike a native dialog); starting a second delete commits any still-pending one first, so at most one is ever in flight per hook instance. Wired into the two C2 named exactly (Projects "Delete", SavedLocations "Remove") plus CustomerData's location/upload deletes (two independent hook instances there — same id could otherwise collide across the two tables). Left ApiKeys' "Revoke" and Connections' "Disconnect" on their existing native `confirm()` rather than converting them — they carry real security semantics (killing live credentials/tokens) where a silent 5s optimistic-hide is the wrong model; got a real `.catch()` added instead (see below). Reviewed button vocabulary (C3) across all destructive actions — concluded the labels are already domain-differentiated on purpose (Remove = unshortlist, Delete = destroy a record, Revoke/Disconnect = domain-specific) and left them as-is rather than force a cosmetic rename. `EmptyState.dependency` wired into the two remaining "needs a project first" empty states that didn't have it yet (Connections, Forecast) — all four now consistent with Reports/CustomerData. `.catch()` swept everywhere: a script-verified pass over every `pages/*.tsx` found and fixed every bare `try{}finally{}` with no `catch` (SavedLocations' status/notes updates, ApiKeys' revoke, three Connections handlers + its per-project `load()`, Reports' share/unshare) plus one real bug — CustomerData's upload-status poller had no catch at all, so a single transient failure mid-poll became an infinite silent-failure loop (fixed: stops polling and surfaces an error instead).
- [x] **A6** Rebuilt "Which locations yield well" in `Forecast.tsx`: PPI/CapEx/payback as `<StatChip>` icon chips (`ti-chart-bar`/`ti-currency-rupee`/`ti-hourglass`), a 4th always-present fixed-size slot for the revenue-cap/catchment-overlap caveat (an amber `ti-alert-triangle` with a native tooltip when active, invisible placeholder otherwise) — chip *count* never varies row-to-row, which is what actually fixes the E3 ragged-height problem, not a CSS height hack. `within_budget` stays a `pill` status lozenge (already distinct, just kept) next to a new `<MicroBar>` scaled to `monthly_revenue / max(monthly_revenue)` across all recommended sites. **Note on "sparkline"**: `ForecastSite` has no per-site time series in the API (`api.ts`'s `ForecastSite` — single `monthly_revenue` number, no history) — a literal sparkline would have had to fabricate a trend that doesn't exist, which conflicts with this codebase's honesty-labelling convention (proxy/sample values get called out explicitly elsewhere, e.g. the Forecast rewrite's SWOT/spider-chart work). Used the already-built `<MicroBar>` (a real relative-magnitude bar, from A1) instead of `<Sparkline>` — same "reachable-revenue at a glance" job, no fabrication. Sites now split into `within_budget` (always shown) and the rest, collapsed behind a "Show N more — need a bigger budget" toggle (`useState`, default collapsed) reusing the existing `.fc-stale-link` treatment.
- [x] **A7** Sidenav regrouped into three labelled sections in `App.tsx` (`NAV_SECTIONS` replaces the flat 12-item `NAV` array) — **Workspace** (Dashboard, Map, Projects, Saved Locations, Forecast, Reports), **Data** (Store Data, Connections), **Account** (Activity, Billing, API Keys). Credits genuinely folded into Billing (N5), not just visually grouped: `Credits.tsx` deleted, its balance/buy-credits/ledger UI moved into `Billing.tsx` as a "Credits" section between the existing "Plan" and "Invoices" sections (each with its own `AsyncBoundary`/error state — a failure loading invoices doesn't blank the credits ledger and vice versa); `/credits` now a redirect to `/billing` (`<Navigate replace>`) so old links/bookmarks still land somewhere real. "Store Data" naming: the user-facing name was already consistent (nav label and page `<h1>` both already said "Store Data") — the mismatch N5 flagged is `/customer-data` (route slug) vs "customer locations" (internal API/type naming), both invisible to a user who navigates by clicking, not typing URLs; renaming either would touch the backend, several hardcoded `ReturnToLink` targets, and risk breaking bookmarks for zero user-visible benefit, so left alone.

### Phase B — Visual production polish  ·  status: **complete (5/5)**

- [x] **B1** Per-project **map thumbnails**: client-side `<MiniMap>` SVG (`workspace/src/components/MiniMap.tsx`) — chosen over a server-side static snapshot because no server-side rasterisation capability exists anywhere in the codebase to build on (`_report_pdf.py`'s PDF generation is `reportlab` tables/text only, no PIL/staticmap/headless-browser), while the two data endpoints needed (`/api/locations`, per-pincode `/api/.../score`) already exist and return small per-project JSON. Plots saved locations scaled to their own lat/lng bounding box; dots tinted by economic score via the existing `ramp("ppi", …)` (same colours the map and SavedLocations already use) when a score is available, else a plain brand-colour dot — never a fabricated trend (same honesty rule as A6/B2). Wired into Dashboard's hero (`map-hero-thumb`, replacing the old hand-drawn placeholder SVG, scores fetched per-pincode bounded to just the active project) and Projects' list rows (new `leading` slot added to `<DataRow>`/`<DataList>`, 48×48, one batched `listLocations()` call grouped client-side by `project_id` — deliberately no per-row score fetch there, to avoid an N+1 fan-out across every project's every location for a thumbnail too small for tint to read anyway). Empty state (no valid lat/lng) renders a neutral placeholder (muted pin glyph on `--paper-3`) rather than a broken-looking blank box. Project detail page (N4) doesn't exist yet (Phase E), so that third placement is deferred until it does. Verified with `tsc --noEmit` + `vite build` (clean) and a static Playwright screenshot of both placements (populated + empty states) against the real built CSS.
- [x] **B2** Micro-viz landed everywhere it could use **real** data without fabricating a trend that doesn't exist (same honesty constraint as A6). Backend: `list_projects` (`paisamap-etl/etl/_auth_db.py`) now returns `location_count`/`report_count` via cheap correlated subqueries (indexed FK, a handful of projects per user) — verified end-to-end through the real Flask app with a local sqlite `test_client` run, not just unit-level. Frontend: those counts render as `<StatChip>` icon chips on Projects, Reports' project-picker, and the top-bar `<ProjectSwitcher>` — directly answers C5 ("bare name-only `<select>` — no '12 locations' context"). Dashboard's Saved-locations/Projects stat tiles get a real "+N this week" delta computed client-side from `created_at` on data already being fetched (no new endpoint needed) — Credits' trend deliberately skipped, since showing it honestly needs the ledger history Dashboard doesn't currently fetch, not a fabricated arrow. SavedLocations' economic-score text got a `<MicroBar>` using the same `ramp("ppi", …)` coloring the map itself uses, so the score reads consistently wherever it appears. Not touched this round: ApiKeys' usage counts, Credits/Billing ledger rows, Activity — real further B2 candidates, left for a future pass rather than force-fitting a viz onto every row this session.
- [ ] **B3** Screen-by-screen redesign to the Forecast bar — "the big one," broken into per-screen sub-items so it ships incrementally like Phase A did, rather than one giant pass:
  - [x] **B3.1** New **Project detail page** (N4/E4) — `workspace/src/pages/ProjectDetail.tsx`, routed at `/projects/:id`. Composes everything built so far into one hub: a `map-hero`-style header (MiniMap at hero size, tinted by score same as Dashboard's, location/report `<StatChip>`s, business_type, website), inline edit (reuses `Projects.tsx`'s now-exported `<BusinessFields>` rather than duplicating the fields grid), delete (blocking `confirm()`, not the optimistic-undo pattern — a project's own detail page can't stay mounted for the 5s undo window since delete navigates away, and `usePendingDelete`'s unmount cleanup cancels rather than commits a still-pending delete, so the undo pattern is actively wrong here, not just a stylistic choice), then four section cards: saved-location shortlist (real per-pincode scores, `<Pill>` status), latest forecast (read from the same `pm_fc_prev_<id>` localStorage key `Forecast.tsx` already writes — real previously-computed data, not fabricated, but honestly not available cross-device/browser), reports (filtered client-side from `listReports()`, no new endpoint), connections (`getConnections`, GA4/Search-Console pills). Two of E4's items deliberately **not** built: "activity" (grepped `_auth_db.log_activity` call sites — only `project_create`/`location_save` are ever actually logged despite Activity.tsx's fuller label set, so a project-scoped feed would be near-always-empty; not worth surfacing a near-broken feature) and "members with access" (Phase D — orgs/roles don't exist yet). Projects list rows now navigate here on click (`onToggle` → `navigate`); inline quick-edit moved off the row-click and onto its own explicit "Edit" button (`e.stopPropagation()`, same pattern the existing Delete button already used) so both stay reachable. `PROVIDER_LABEL` (GA4/Search Console display names) was a private duplicate inside `ProjectWizard.tsx`; promoted to a real export on `lib/api.ts` next to `ConnectionProvider` and both pages now import the one copy. Screenshot QA (not just tsc) caught a real layout bug: 4 buttons (map/forecast/edit/delete) crammed into the hero's action row wrapped mid-word at normal page width — fixed by moving Edit/Delete out to lightweight `.icon-link` text-buttons in the card's top-right corner, keeping the hero-actions row to the two primary nav CTAs it was actually designed for. Verified `tsc --noEmit` + `vite build` (clean) and Playwright screenshots of both the detail page (populated + hero-fix before/after) and the updated Projects row.
  - [x] **B3.2** Dashboard (`workspace/src/pages/Dashboard.tsx`) — hero/thumbnail were already done (A4/B1/B2), fixed the two real gaps: **(1)** the "Recent activity" card had never gotten A2's `<AsyncBoundary>` treatment — `api.listActivity(5)` had no `.catch()` at all, so a failed fetch left it silently "Loading…" forever, the exact silent-infinite-load bug A2 fixed everywhere else but missed here; now has a real `activityError` state, retry, and — since it's actually a list of rows, not a stat tile — renders through `<DataList>`/`<DataRow>` + `<StatChip icon="ti ti-clock">` instead of a hand-rolled `<ul><li>` with inline flex styles, matching Activity.tsx's own B4 treatment exactly (same page, same pattern, not a new one invented for Dashboard). **(2)** `api.listLocations()` also had no `.catch()` — added one so a failure degrades the stat tile to `0` instead of an unexplained permanent `—`. Also added a "View all →" link on the activity card (the section-header pattern B3.1 introduced) and made all four `.stat-row` tiles real `<Link>`s (Plan/Credits→`/billing`, Saved locations→`/locations`, Projects→`/projects`) via a new `.stat-tile-link` modifier class (kept separate from bare `.stat-tile`, not applied globally, since Billing's own non-clickable Credits tile shares the same base class — didn't want a hover affordance implying clickability where there isn't one). Deliberately left `.stat-tile` itself unstyled/icon-free — it's an intentionally distinct "big number" pattern already shared identically with Billing (B3.7), and giving Dashboard's copy icons while Billing's stays bare would be a new inconsistency, not a fix; that's B3.7's call to make for both at once. Frontend-only. Verified `tsc --noEmit` + `vite build` (clean) and a Playwright screenshot of the stat-tile hover state and the rebuilt activity feed.
  - [x] **B3.3** SavedLocations (`workspace/src/pages/SavedLocations.tsx`) — closed the pre-existing bug B5 flagged and deliberately left alone: `.data-row-trailing select` (the per-row status dropdown) was stacking under its "Remove" button instead of sitting beside it, same root cause as B5's `.action-bar-buttons` fix (the global `select{width:100%}` rule inside a flex row) — added the same `width:auto` override, now scoped to `.data-row-trailing select` globally (checked first: SavedLocations is the only page with a bare `<select>` in a trailing slot, so this is a safe, non-speculative fix, not a shot in the dark). Rows are now click-through to the map for that pincode (`onToggle` → `/map?pincode=…&project_id=…`), matching the session's now-established "rows navigate, editable controls stay isolated" pattern from B3.1's Projects change. Doing that surfaced a real latent bug in the shared `<DataRow>` itself: its `leading` slot (B1's checkbox/thumbnail slot) had never gotten the `stopPropagation` guard `trailing` already had, so a click on SavedLocations' new bulk-select checkbox would have bubbled up and navigated the row away instead of just toggling the checkbox — fixed once in `DataList.tsx`, benefiting every current and future `leading` use, not patched around per-page. Frontend-only. Verified `tsc --noEmit` + `vite build` (clean) and a Playwright screenshot confirming the select/button now sit inline.
  - [x] **B3.4** Reports (`workspace/src/pages/Reports.tsx`) — small pass: "Generate for a project" header switched from raw inline mono/uppercase styling to `.kicker` (same fix as Dashboard/ProjectDetail); the reports list itself was the one section on this page with no `.card` wrapper at all (floating directly on the page background while "Generate for a project" above it was card-wrapped) — gave it one with a "Your reports" kicker, matching the two-labelled-section pattern Billing already established for Plan/Credits/Invoices. Checked whether "which project does this report belong to" was a real gap (every other page this session got a project link-through) — it isn't: `blueprints/reports.py`'s default title is already `"{project.name} — Location Intelligence Report"`, so the project is already named in every report row; no fix needed. Frontend-only. Verified `tsc --noEmit` + `vite build` (clean) and a Playwright screenshot of the two-card layout.
  - [x] **B3.5** CustomerData (`workspace/src/pages/CustomerData.tsx`, the longest page — upload, locations, drivers, expansion recommendations, digital-signal tags, past uploads). Found a real bug, not just a consistency gap: the mapping-preview table's cell borders used `border-bottom:1px solid var(--line)` — `--line` is never defined anywhere in `styles.css`, so the invalid custom property made every border resolve to `none`; the table's rows had no visible separators at all. Fixed to `var(--border)`, the token every other bordered element in the app actually uses. Consistency: 5 section headers were raw inline mono/uppercase `<p style={{...}}>` duplicating `.kicker` (added one to "Upload store data" too, which had no header at all) — all switched to `className="kicker"`. Two sections — the main store-locations list and "Past uploads" — had never been wrapped in a `.card` at all, floating directly on the page background while every sibling section was card-wrapped; both fixed. Frontend-only. Verified `tsc --noEmit` + `vite build` (clean) and a Playwright screenshot confirming both the card-wrapping and the now-visible table borders.
  - [x] **B3.6** Connections (`workspace/src/pages/Connections.tsx`) — the same `var(--line)` bug found in B3.5 turned out to be a copy-pasted pattern, not a one-off: this page's `thStyle`/`tdStyle` shared style objects (used by both the ecommerce top-items table and the GA4/GSC report table) had the identical undefined-token border bug. Fixed both, then grepped the entire `workspace/src` tree for `var(--line)` to confirm no third copy exists anywhere — clean. Rest of the page (provider cards with `<h3>` titles, the consent gate card, shared `thStyle`/`tdStyle` objects) was already internally consistent and didn't need the kicker/card-wrap changes B3.5 needed — `<h3>` is the right call here since each card names one specific integration (GA4, Search Console), not a generic section label, so left as-is rather than force a pattern that doesn't fit. Frontend-only. Verified `tsc --noEmit` + `vite build` (clean); no new screenshot needed — same border fix already visually confirmed working in B3.5's CustomerData table.
  - [x] **B3.7** Billing (`workspace/src/pages/Billing.tsx`) — closed the deferred B3.2 question for good: decided `.stat-tile` stays bare/icon-free everywhere (Dashboard's 4 tiles and Billing's single Balance tile now both intentionally icon-free, not one ahead of the other). Real gap: "Buy credits" had the same raw-inline-style-instead-of-`.kicker` issue as every other page this pass; fixed. Bigger gap: the credit-history and invoices `<DataList>`s were the only two sections on this 3-section page (Plan / Credits / Invoices) with no `.card` wrapper — floating bare while "Buy credits" right above the ledger was already card-wrapped, and the top-level "Plan"/"Credits" kickers correctly stayed outside any single card since they head a multi-card grid, not one list (a real, different, valid pattern — not a gap to fix). Both now card-wrapped with their own kicker, matching Reports'/CustomerData's now-established "each section = one card" rule for pages that combine several distinct concerns (vs. single-purpose list pages like Projects/SavedLocations, which correctly stay bare). Frontend-only. Verified `tsc --noEmit` + `vite build` (clean) and a Playwright screenshot of the full page.
  - [x] **B3.8** Activity + ApiKeys — **B3 now complete, 8/8**. ApiKeys (`workspace/src/pages/ApiKeys.tsx`) was already at the bar on inspection — correct `.kicker` usage, the one genuinely action-oriented section ("Create a new key") already card-wrapped, "Revoked" left as a light bare sub-list rather than forced into its own card (a filtered view of the same key list, not a distinct data type like Billing's Plan/Credits/Invoices, so it doesn't fit the multi-section-card rule) — no changes needed. Activity.tsx had a real bug, not just a styling gap: its own `ACTION_LABELS`/`describe()` were a second, independently-drifted copy of the identical pair in Dashboard.tsx's mini feed (added back in B3.2) — Activity's copy was missing `location_score`/`location_compare`/`forecast_run`/`report_generate`, so the one page meant to be the complete activity history would have shown raw backend action strings (e.g. `"forecast_run"`) instead of "Ran a forecast" for exactly the entries Dashboard's smaller feed already humanized correctly. Deduplicated into one `workspace/src/lib/activity.ts` (`ACTION_LABELS` + `describeActivity()`), imported by both pages, using Dashboard's fuller 7-entry set as canonical. Frontend-only. Verified `tsc --noEmit` + `vite build` clean.

  **B3 wrap-up**: across B3.1–B3.8, found and fixed 3 real bugs beyond pure consistency work — the `var(--line)` undefined-CSS-token bug (silently killed table borders in 2 different pages, confirmed via a full-tree grep that no 3rd copy exists), `<DataRow>`'s `leading` slot missing the `stopPropagation` guard `trailing` already had (would have broken SavedLocations' new bulk-select checkbox), and the Activity/Dashboard `ACTION_LABELS` drift. Established (not just applied) two durable rules for future pages: single-purpose list pages (Projects, SavedLocations, Activity, ApiKeys) stay bare-list, no card; multi-section pages (everything else) get one `.card` + `.kicker` per section. `.stat-tile` stays icon-free everywhere, decided once for both pages that use it rather than left ambiguous.
- [x] **B4** New `<Pill>` component (`workspace/src/components/Pill.tsx`) auto-resolves a tone-matched Tabler icon (approved=check, rejected=x, reviewing=clock, shortlist=circle-dot, delta-pos=trending-up, delta-neg=trending-down) from the same 6-tone vocabulary every page already reduces its status to via a lookup table (`RISK_CLASS`/`STATUS_CLASS`/`GEOCODE_CLASS`/`CONF_CLASS`/`UPLOAD_STATUS_PILL`/…) — one shared mapping instead of picking an icon at each of the ~25 call sites across ApiKeys, Connections, CustomerData, Forecast, ProjectWizard, Reports, forecastCharts, Billing. `.pill` CSS switched `inline-block`→`inline-flex` to hold the icon. Plan-tier badges (`pill plan-free/pro/team`) deliberately left untouched — identity, not status, a different vocabulary. Second half: every remaining bare `.meta` text span (unlabelled dates/amounts on Activity, Billing's ledger balance + invoice amount, Projects' updated-at, CustomerData's upload date, Reports' created-at) converted to `<StatChip>` with an icon (`ti-calendar`/`ti-clock`/`ti-wallet`/`ti-currency-rupee`) — now every trailing fact in every list is a labelled, iconed chip, no more bare text landing next to real chips. `.data-row .meta` CSS rule deleted once its last call site was gone (grepped the full src tree first to confirm). Frontend-only, no backend/schema touched. Verified `tsc --noEmit` + `vite build` (clean) and a static Playwright screenshot of all 6 pill tones plus two real row compositions (Reports row, Credits ledger row).
- [x] **B5** Scoped with the user first: the map's own Compare popup (compareBar chips + modal table) lives in the standalone vanilla-JS map app (`index.html`), a separate codebase from the React workspace, so it can't literally become a shared React component — went with (a) extract the one real filter-select pattern that already existed in the workspace, and (b) add a genuine new bulk-action-bar use case rather than build one with no call site. **(a)** New `<FilterBar>`/`<FilterSelect>` (`workspace/src/components/FilterBar.tsx`) extracted from Forecast's `HotspotBubbles` "Colour by / X axis / Show" filters (`forecastCharts.tsx`) — `.fc-filters` CSS renamed to `.filter-bar` (only call site, no back-compat class needed). **(b)** New `<ActionBar>` (`workspace/src/components/ActionBar.tsx`), same white/border/shadow language as the map's own `.map-action-bar`/`.compare-basket`, wired into SavedLocations: rows get a checkbox (new `leading` slot content, reusing B1's slot), and selecting any show a "N selected" bar with bulk status-change and bulk delete (native `confirm()`, matching A5's precedent that a multi-item destructive action gets a blocking confirm rather than the 5s optimistic-undo used for single-row deletes). Found and fixed two real bugs along the way: `.action-bar`'s pill radius (`999px`, copied from the map's fixed-width floating bar) turned into a broken giant oval once content wrapped to two lines at typical page widths — switched to `var(--radius-lg)` matching `.map-action-bar`'s own precedent; and the global `select{width:100%}` rule made the bulk-actions' `<select>` blow out onto its own line inside the flex row — added `.action-bar-buttons select{width:auto}` (same override pattern already used at `.fb-chip select`/`.topbar-switcher select`). Both caught by an actual rendered screenshot, not just code review — first pass looked fine as markup but broke visually. **Noted, not fixed** (pre-existing, out of scope): the same `width:100%` issue affects `.data-row-trailing select` too (e.g. SavedLocations' own per-row status dropdown stacks under its "Remove" button instead of sitting beside it) — a real B3/B4-adjacent cleanup item for whenever DataRow's trailing selects get touched next. Frontend-only. Verified `tsc --noEmit` + `vite build` (clean, 3 iterations after the two visual fixes) and Playwright screenshots at both normal and narrow width.

### Phase C — Company → Project hierarchy  ·  status: **complete, C1-C5 all shipped and live in production**

- [x] **C1** Decided without a separate brief-and-sign-off round — most of it turned out to already be locked by `docs/PRICING_MODEL.md` rev 6 (company is the billing unit; 1 company included per paid tier; the roadmap's own C3 wording already assumed "wrap every existing user's projects in an auto-created default company", i.e. every user gets one, not just teams — the schema's `org_id` "NULL = personal project" comment predates that and is now stale). The one real gap found and closed with the user directly: Phase C as originally scoped (C2–C5) never moved `credits_ledger`/`users.plan` to the company level, but `PRICING_MODEL.md` P1 requires exactly that ("plan + credits + seats attach to a company") — **scope expanded** to include it. Migration approach: **staged, additive-first** — build the container+membership layer with zero behavior change before ever touching how existing plan/credits/ownership resolve, given how much of the payment surface (orders, invoices, Razorpay webhooks, `api_keys` tier resolution) is wired straight to `user_id` today and getting that cutover wrong touches real money.
- [x] **C2** `blueprints/organizations.py` (list/create/get/update/delete + member list/add/update-role/remove, all role-gated: owner/admin can manage, any member can view, last-owner can't be removed or demoted) + matching `_auth_db.py` functions (new "Organizations (Phase C, staged)" section). Schema, additive only: `organizations.plan`/`website_url` columns added; `org_id` (nullable, unenforced-default) added to `credits_ledger`/`customer_uploads`/`customer_locations`/`oauth_connections` — every one of them stays NULL until a later, separate cutover actually writes to it, so **zero existing behavior changes** (users.plan/credits/project ownership all still resolve exactly as before). Both the new `_MIGRATIONS` entries (idempotent `ALTER TABLE ADD COLUMN`, the established pattern — a fresh `TEXT DEFAULT 'free'` compound-type entry verified directly against SQLite since every prior entry was a bare type) and the `Table()` defs (for `init_schema()` on fresh installs) updated together, matching the file's existing dual-registration convention. Verified with a full local Flask `test_client` pass (real session auth, real sqlite DB, 19 assertions) covering create/list/get/update/delete, membership add/promote/remove, and every permission edge: non-member blocked, member blocked from owner/admin actions, duplicate-member and unknown-email guarded, last-owner protected on both remove and demote — plus confirmed the new `org_id` columns exist and stay NULL for a freshly-created project/credit-grant. Not yet done: no frontend UI for this (deliberately — see C4, which needs C3's migration first to have real data to scope).
- [x] **C3 — shipped and run against production.** `_auth_db.backfill_organizations()` + `paisamap-etl/db/backfill_organizations.py` (matches the existing `migrate_csv_to_db.py`/`init_auth_schema.py` script convention exactly, idempotent, prints a summary). For every user with no org yet: creates one ("`<name or email-local-part>`'s Workspace"), adds them as owner, carries `users.plan`→`organizations.plan` and the first project website_url found→`organizations.website_url`; stamps `org_id` on every existing `projects`/`oauth_connections`/`customer_uploads`/`customer_locations`/`credits_ledger` row that doesn't have one. Verified locally first (real sqlite, 3 runs, multi-project/zero-project/no-name-user/idempotency/post-migration-new-activity scenarios, plus connections/uploads/locations separately) — see the session log below for that pass. **Deployed and run for real** (2026-09-12): committed + pushed to `main` (`959d52b`, rebased cleanly past 2 unrelated bot enrichment-sync commits), confirmed the `Deploy PaisaMap` Actions run went green, confirmed `/api/organizations` live (401 for an unauthed request, not 404 — the route exists), took a read-only pre-migration snapshot over SSH (1 user, 4 projects, 0 orgs — genuinely small, low blast radius), ran the real backfill against prod (`org_id`s stamped: 4 projects, 6 oauth connections, 12 credit-ledger rows, 1 org created), verified the actual resulting rows (org name/plan/website_url, membership, all 4 projects' org_id) match what the code should produce, re-ran it a second time against prod to reconfirm the idempotency guarantee holds for real (not just locally) — zero writes, and did a final smoke check that `/` and existing endpoints are completely unaffected. Zero live-behavior change, as designed — this only prepared data.
- [x] **C4 (partial by design)** `<CompanySwitcher>` in the topbar (next to `<ProjectSwitcher>`, same shared `.topbar-switcher` markup/CSS) + a new `/company` **Company Settings** page: rename/website edit, member list with role-change/remove (owner-only), add-member-by-email (owner/admin, with friendly errors for the real failure modes — unknown email, already a member, last-owner protection), create-additional-company, and delete-company (blocked from deleting your only one). `WorkspaceProvider` (`lib/workspace.tsx`) extended with `organizations`/`activeOrgId`/`setActiveOrgId`/`activeOrg` alongside the existing project state — same shape, same localStorage persistence, so every page that already calls `useWorkspace()` gets company context for free. **Real signup-time default org**: every new signup now gets a default company too (`blueprints/auth.py`, mirrors C3's backfill logic exactly, refactored into one shared `_auth_db.create_default_organization_for_user()` both call) — otherwise every account created after C3's one-time backfill would start with zero companies, silently reopening the gap C3 just closed. New projects now get tagged with the active company at creation time (`create_project` gained an `org_id` param — falls back to the caller's own primary org if omitted or if the given org_id isn't one they're actually a member of, never errors or silently leaks another user's org). **Deliberately not done — "project list scoped to active company" filtering**: every real account today has exactly one company, so there is no second company to verify that filter against yet; building unverifiable filtering logic now (with the exact risk of an untested edge case silently hiding a user's own projects) was judged worse than shipping the switcher + settings + tagging now and adding the filter once a second real company exists to test it against — same "don't build for a scenario with no real test case" discipline as C1's other deferred pieces. Verified: `tsc --noEmit` + `vite build` clean; a local Flask `test_client` pass covering create_project's org_id fallback logic (explicit valid org, no org given, someone else's org, a malformed org_id string via the real HTTP body — none of them error, all fall back correctly) and a regression check on the C2 organization endpoints; **a full end-to-end test of the real `/api/auth/google` signup route** (Google's own token verification patched out, not skipped) confirming the new default-org side effect coexists correctly with the existing signup-bonus credit grant and activity log — this is the most sensitive path touched this phase, so it got the most scrutiny, not just a unit-level check; a Playwright screenshot of the topbar + full Company Settings page.
- [x] **C5** Re-point Forecast/expansion/reports/store-data/connections — real production data revealed the original wording ("connections shared across the company") was actually wrong for how this account uses projects, so this ended up as two different fixes, not one, worked out with the user directly before writing any code:
  - **Checked real production data first, not just design intent**: found the one real company's 4 projects each had their own distinct GA4 property / Search Console site (different businesses/clients tracked under one company, not one business viewed from different project angles) — the exact opposite of C5's original "share one connection across the company" premise. Store data (`customer_uploads`) was empty everywhere, so no such conflict existed there.
  - **Store data**: shares across the company after all (user's call, despite the same distinct-businesses pattern applying) — `create_customer_upload`/`create_customer_locations_bulk` now stamp `org_id` at creation, and `list_customer_uploads`/`list_customer_locations` resolve by the requesting project's `org_id` rather than its own `project_id` when one is given. Every real caller (CustomerData, expansion, forecast, the GA4 location-tagging join) already always passes a `project_id`, so this took effect everywhere without any blueprint changes beyond the two `_auth_db` functions. Write actions (delete/update on a specific upload) deliberately stay owner-only for now — real multi-member permissions are Phase D, not built.
  - **Connections**: user's real usage showed per-project connections are often intentional (distinct businesses), so instead of collapsing to one shared connection, a company can now hold **multiple** connections per provider and each project explicitly **picks** which one it uses — genuinely new infrastructure, not a query tweak: a new `project_connection_selections` table (project_id, provider, oauth_connection_id) decouples "who created a connection" from "who's using it." `get_connection_for_project`/`select_org_connection_for_project`/`list_org_connections` resolve/switch/pool; `mark_oauth_connection_tokens`/`mark_oauth_connection_error`/`update_oauth_connection_ref` re-keyed from `(project_id, provider)` to a connection's own id (the actual row in use may not be the requesting project's own). `disconnect_project_connection` only really deletes + revokes at Google when no other project is still using that exact connection — otherwise it just unlinks this project, leaving a teammate's still-shared connection untouched. `_get_valid_access_token` (the single choke point every GA4/GSC read — report, ecommerce, location-tags, PDF digital-baseline — already goes through) needed exactly one change to make all of them company-aware at once. New `POST .../connections/<provider>/use` endpoint + `Connections.tsx` picker ("use a different company connection" / "or use one already connected in your company") on both the connected and not-yet-connected states.
  - **`backfill_connection_selections()`** (mirrors C3/C3-style scripts): gives every pre-C5 connection an explicit selection linking it back to its creating project (preserving today's exact behavior) and stamps `org_id` where still missing. Needed because production has 3 real, live, encrypted-token connections that predate this table.
  - Verified with real rigor given real tokens were at stake: a local test replicating the exact production shape (1 org, 3 projects, each with its own GA4 connection) covering default resolution, pool listing, switching, cross-org selection rejection, both disconnect paths (shared→unlink-only vs sole-use→real delete+revoke, then confirmed a live teammate connection survives the unlink), reconnect-after-disconnect, the backfill's idempotency, and store-data cross-project sharing + cross-org isolation — plus the same scenarios again through real Flask HTTP routes (not just the `_auth_db` functions directly), plus a full C2–C4 regression pass since this round edited shared `_auth_db.py` code extensively. A Playwright screenshot of the new picker caught one more real bug: the global `select{width:100%}` issue (same class as B3.3/B5) hit these two new selects too; fixed with inline `width:auto`.

### Phase D — Users, roles & permissions  ·  status: **complete, D1-D4 all shipped and live in production**

- [x] **D1** Email invite flow (invite token, accept/decline) — the "blocked on missing infra" note below no longer applies: resolved by not actually needing the vendor decision yet. New `org_invites` table + full lifecycle (`create_or_resend_org_invite`/`list_org_invites`/`revoke_org_invite`/`accept_org_invite`/`decline_org_invite`) in `_auth_db.py`, routes in `blueprints/organizations.py`, and an SES-backed `_email.py` that degrades gracefully exactly like `blueprints/billing.py`'s Razorpay `_client()` does when unconfigured — `email_sent: false` and the `accept_url` returned directly in the API response as a fallback, so an invite is fully usable (copy/paste the link) even with zero AWS setup. `org_members.role` stays owner/admin/member (3 roles) per the D2 decision, not the 4-tier owner/admin/editor/viewer this bullet originally proposed. `InviteAccept.tsx` works both pre- and post-sign-in (rendered standalone by `App.tsx` before the auth gate, same pattern later reused for E2's public share page). Verified with a real local Flask `test_client` pass (full lifecycle incl. resend-rotates-token, email-mismatch-on-accept, permission checks) and — since this touches real user accounts and a real database write — a real prod-DB smoke test after deploying (create→list→public-token-lookup→revoke against the actual live database, explicit user approval obtained first since Claude Code's auto-mode classifier gates prod DB mutations by default). Deployed and schema-migrated live (`a6512e6`).
- [x] **D2** The actual permission layer — not literally "middleware in `_session.py`" as originally sketched, but a `_project_role()`/`PROJECT_DELETE_ROLES` check reached through the existing `get_project` keystone every project-scoped function already calls, which turned out to be the higher-leverage design: one change there propagates correctly everywhere without a separate per-route middleware layer to keep in sync. **Real gap found before writing code**: checked, and zero project/location/report/upload/connection endpoints did any org-role check — every one still gated on `user_id == creator`, meaning a "member" added via Company Settings (C4) could manage the company page itself but couldn't see or touch a single one of its actual projects. That's now fixed. Locked the read/write/delete matrix with the user first (a real product decision, not an implementation detail): **member** = read + create/edit everything in the company (projects, locations, reports, store data, connections) but can't delete anything or disconnect a connection; **admin** = all of that plus delete; **owner** = all of that plus deleting the company itself (already true from C2/C4). Implementation: `get_project` (the keystone — nearly every other function's ownership check routes through it) now returns the project for the creator OR any org member, with an attached `role`; `list_projects`/`list_saved_locations`/`list_reports` (no `project_id` given) now return everything across every company the user belongs to, not just what they personally created; every delete function (`delete_project`, `delete_saved_location`, `delete_customer_upload`, `delete_customer_location`, `disconnect_project_connection`) now requires `_project_role() in ("owner","admin")` specifically. `Project`/`Organization` API responses both now carry the caller's `role` (frontend type added, not yet used to hide UI — see below). Found and fixed two real bugs along the way, unrelated to the permission work itself but surfaced by touching this code: (1) `count_unresolved_locations`/`list_pending_geocode_locations` would have silently shown a **0 count** for a teammate's upload once uploads started being company-shared (C5) — they filtered by the *caller's* `user_id` on the locations table instead of scoping via the upload itself; (2) `set_report_share_token` never returned the updated row (fell through to an implicit `None`) even though both blueprint callers (`share_report`/`unshare_report`) jsonify it directly — `/api/reports/<id>/share` and `.../unshare` had *always* responded with `{"report": null}` in production. Deliberately deferred: hiding Delete/Disconnect buttons in the UI for a plain member — there is exactly one real member (the owner) in production today, nothing to verify that UI change against yet; the backend enforces the policy regardless, so this is a pure UX-polish gap, not a security one. Verified with the most rigorous test suite of this whole phase, matching the stakes (this governs every piece of user data now): a real multi-member scenario (1 org, owner + admin + member + a completely unrelated outsider org) covering read/write/delete across every resource type, confirming a blocked delete attempt actually deletes nothing (not just returns false), an outsider gets the same non-disclosure 404 as a genuine not-found, connections' disconnect specifically gated at delete-tier, and — critically — a full regression pass proving the single-owner case (today's actual production shape) behaves byte-for-byte identically to before this shipped. `tsc --noEmit` + `vite build` clean.
- [x] **D3** Member-management UI — already shipped as part of C4 (add-by-email, change role, remove, all under Company Settings); the only piece D3 originally wanted that's still missing is the true "invite someone with no account yet" flow, which is D1's email-infra gap, not a UI gap.
- [x] **D4** Data-access audit log, admin-visible — checked the real gap before writing code: `/api/export` (the literal "export" in "who viewed/exported what") had **zero** logging of any kind since it predates the whole auth stack, and `blueprints/reports.py`'s `download_report`/`view_shared_report` (the public, tokened share-link view) were the same — never logged despite `report_generate` already being. Closed all three via a new `log_data_access()` helper in `_session.py` that resolves identity exactly like `get_effective_plan()` does (X-API-Key first, then session, else anonymous) and writes to the existing `activity_log` table — no schema change needed. New `GET /api/organizations/<id>/audit-log` (owner/admin only, same forbidden-shape as the other org endpoints) surfaces a deliberate **subset** of `activity_log` — `DATA_ACCESS_ACTIONS` (export/report-generate/download/share-view/location-score/location-compare/customer-upload) — not the full personal feed; login/purchase/connection-connected stay off this view since they're account events, not data access. Wired into a new "Data-access audit log" card on Company Settings, gated the same `isOwnerOrAdmin` way the Members card already is. **Known, accepted limitation**: a fully anonymous `/api/export` call or share-link view has no `user_id` to attribute to any org's roster, so it's logged (visible in server-side aggregate) but won't appear in any specific company's audit log — the same "don't build unverifiable attribution logic" discipline C4/C5 already established, since production has exactly one org to test a fancier report→project→org join against, not a real second case yet. Verified with a real Flask `test_client` HTTP pass (not just calling the DB function directly): a real signed-in member's `/api/export` call correctly attributes to them, an anonymous call logs with no `user_id` and is correctly invisible to the org's audit log, an outsider and a plain member both get a real 403 on the new endpoint, `login` is correctly excluded from the filtered view, and the single-owner-only case (today's actual production shape) returns cleanly with zero entries — no regression. Also did a real local-browser Playwright screenshot of the new Company Settings card (own Flask + Vite dev servers, a minted session cookie, seeded sqlite data) — renders correctly, no console errors. `tsc --noEmit` + `vite build` clean. **Deployed and verified live** (`4ed0c93`/`5896e47`, rebased past a same-day cron data-sync commit): confirmed the deploy went green, `GET /api/organizations/1/audit-log` returns a real 401 (not 404 — the route exists) for an unauthenticated request, `/api/export` still returns 200 (the new logging call didn't break the live endpoint), and a read-only SSH check calling `list_org_audit_log` directly for the real production account showed real entries — the account's own recent `location_score` history from actually using the map, correctly attributed — plus confirmed a non-member user_id gets `None` (the 403 path) against real prod data.

### Phase E — Project-level actions  ·  status: COMPLETE, 4/4  ·  unblocked by C+D

- [x] **E1** Link generated reports to the project; show them on the Project detail page; "Save report" from Forecast/Compare. Reports already had `project_id` and already showed on the B3.1 Project detail page; Compare already had "Save report" (`CompareModal.tsx`). The one real gap was Forecast — added the same `generateReport` call there, deliberately without Compare's bulk-save-candidates step first (Forecast's sites are a dynamic what-if over the whole addressable market, not a small deliberately-picked set — silently turning "within budget" sites into permanent saved locations would be a surprising side effect; the report just uses whatever the project has already saved, same as Reports.tsx's own flow).
- [x] **E2** Share icon on a project. Realized "share with named company members" is already satisfied structurally — any org member already sees every one of the company's projects per D2's read policy, so inviting them (D1) IS sharing. The real net-new piece: a public read-only project link, reusing the report `share_token` pattern exactly (`projects.share_token`, owner/admin-gated `POST`/`DELETE /api/projects/<id>/share`, public `GET /api/projects/shared/<token>`). Public view is deliberately thin — no org_id/user_id/financial fields, no report ids (a report's own download stays behind its own separate, individually-revocable share link, not implicitly opened by sharing the project), archived projects excluded even if a token exists.
- [x] **E3** Manage project: rename (already existed) / **duplicate** / **archive** / delete, all with confirm+undo. Archive is additive-only (`archived_at`), excluded from the default list, any org member can toggle it (same write-bar as editing, not delete's stronger owner/admin bar — archiving doesn't touch data or anyone's access). Duplicate clones the business-profile fields only, not saved_locations/reports/connections (those belong to the specific expansion effort, not the reusable template). Delete now goes through A5's actual undo pattern instead of `window.confirm` — found and fixed the resulting subtlety that the Project *detail* page can't just make a row vanish like the list does, so it stays put showing the undo toast and only actually deletes+navigates-away once the 5s window lapses.
- [x] **E4** Project detail page hub — map thumbnail/locations/forecast/reports/connections already shipped in B3.1; added the last missing piece now that Phase D exists for real: a "Members with access" section (purely informational — every member already has access per D2, this isn't a per-project ACL to edit). "Activity" stays deliberately out, same reasoning as B3.1 — the backend only ever logs 2 of 7 possible action types, so a project-scoped feed would be near-always-empty.

Found and fixed one real bug along the way: `ProjectDetail.tsx` sourced its project from `WorkspaceProvider`'s shared `projects` list, which excludes archived ones by default (correctly, for the nav/switcher) — meaning archiving a project made its OWN detail page 404 "Project not found", including the Restore button that would undo that. Fixed with a direct `GET /api/projects/<id>` fallback fetch (new `api.getProject`) that's now the authoritative source for the detail page, while the shared list stays the fast, no-extra-request path for the common case.

Verified: a full local Flask `test_client` + direct-function pass covering a **real multi-member scenario** (owner/admin/member/outsider), not just today's single-owner shape — archive/unarchive open to members, blocked for outsiders; duplicate excludes saved locations, blocked for outsiders; share/unshare owner-or-admin only, blocked for a plain member; public view correctly omits internal ids, 404s on a bad or revoked token, and excludes an archived project even with a live token; HTTP-route-level checks for all of the above including the public route needing zero session. Also a full local-browser Playwright pass (own Flask+Vite dev servers, seeded sqlite with 3 real org members) screenshotting the Projects list (archived toggle, Duplicate/Archive/Delete buttons), Project detail (hero actions, Share panel open, Members section), the archived project's own detail page (the bug above, confirmed fixed), the delete-undo toast firing and being undone, and the public share page both succeeding and 404ing on a bad token — all rendered correctly, no console errors. `tsc --noEmit` + `vite build` clean throughout. **Not independently screenshot-verified**: the Forecast "Save report" button's own rendered state, since producing a real `sufficient_data: true` forecast result requires a full national candidate-pincode dataset this ad hoc local QA sqlite didn't have — the button reuses Compare's exact already-shipped, already-tested pattern and calls the same independently-verified `generate_report` backend function, so this is a low-risk, code-reviewed-not-pixel-verified gap, not skipped verification.

### Phase F — Demand intelligence  ·  status: not started  ·  can start after C

- [ ] **F1** `signal_history` table — snapshot key signal values per pincode on the enrichment cadence (start capturing now even if UI comes later).
- [ ] **F2** **Existing demand trend** — category/geo demand index from GSC keyword volume + UPI txn value + footfall-proxy trend over `signal_history`.
- [ ] **F3** **Demand creation opportunity** — where modelled purchasing power outruns current commercial supply (underserved gap).
- [ ] **F4** **Demand gen gap** — connected GA4/GSC actual demand vs modelled reachable demand for the catchment.
- [ ] **F5** **Demand forecast** — demand-side projection alongside `_forecast_model` (trend × seasonality × the project's levers).
- [ ] **F6** A "Demand" section on the Project detail page with these four.

### Phase G — Signal synthesis & enrichment engine  ·  status: not started  ·  can start after C

- [ ] **G1** Signal → output coverage matrix (doc + a live internal dashboard): every one of the ~30 signals mapped to which customer output consumes it, both directions. Flag dead weight.
- [ ] **G2** Wire the dormant connected data in: GSC keywords + GA4 geography → demand (F) + suitability + forecast calibration.
- [ ] **G3** Bidirectional loop — anonymised re-synthesis of customer store performance + connected analytics into the shared enrichment set (gated by G-consent + H4 k-anonymity).
- [ ] **G4** Scheduled "self-intelligence" jobs — periodic capture-model re-fit, demand-index refresh, per-customer nudge generation ("your best-performing signal shifted", "a new hotspot appeared near your catchment").
- [ ] **G5** Consent framework for data re-use — explicit opt-in per company, revocable, with a plain-language explanation of what is shared and how it is anonymised.

### Phase H — Security & data protection  ·  status: not started  ·  runs alongside C–G

- [ ] **H1** Column-level encryption for sensitive customer fields (revenue / rent / capex / address) via the `_token_crypto` pattern; key management + rotation plan.
- [ ] **H2** Automated cross-account isolation test suite — every endpoint × every table, a second user must never read/write the first user's rows (or another company's).
- [ ] **H3** Data-access audit log (shared with D4).
- [ ] **H4** Anonymisation / k-anonymity guarantees + a re-identification risk review for the G3 pipeline.
- [ ] **H5** Rate limiting on all mutating endpoints, secrets rotation, dependency-vuln scan, a pen-test checklist; close `TODO.md` § "Security hardening".

---

## 4. Session log

| Date | Session did | Phase items touched |
|---|---|---|
| 2026-09-11 | Full audit; wrote this doc + published tracking artifact. | — |
| 2026-09-11 | Drafted `docs/PRICING_MODEL.md` — the pricing/credits model depends on Phase C (companies) + a billing-v2; see that doc §11 for P1–P9 sequencing. | C (context) |
| 2026-09-11 | Shipped A1: `DataList`/`DataRow`/`StatChip`/`MicroBar`/`Sparkline` primitives, retired `.project-list`/`.list` CSS, migrated all 9 list call sites. Verified with `tsc --noEmit` + `vite build` (both clean) and a static Playwright screenshot of the new markup against `styles.css` (no local backend/auth session available to click-test the live pages). Next: A2 (null/empty/loading/error `<AsyncBoundary>` system). | A |
| 2026-09-11 | Shipped A2: `AsyncBoundary` + `RowSkeleton`/`CardSkeleton`, wired into 10 pages' top-level list/project gates (see A2 note for the full list and what was deliberately left out). Verified with `tsc --noEmit` + `vite build` (clean) and a static Playwright screenshot of the skeleton/error states. Next: A3 (global project context / persistent switcher) — the highest-leverage remaining item since N1/N2/C5 all trace back to the lack of one. | A |
| 2026-09-11 | Shipped A3: `WorkspaceProvider` + `ProjectSwitcher` in a new persistent top bar, deep-link sync, migrated Forecast/Connections/CustomerData off their own per-page selects onto shared context. Verified with `tsc --noEmit` + `vite build` (clean) and a static Playwright screenshot of the top bar. Company-level context deferred to Phase C (nothing to switch between yet). Next: A4 (breadcrumb / "continue your journey" / Dashboard resume rail) or A7 (nav restructure) — both smaller, more isolated items than A3 was. | A |
| 2026-09-11 | Shipped A4: `ReturnToLink`/`ReturnBanner` wired into the exact N2 cases (Forecast→Store Data, Wizard→Connections/Store Data); found Dashboard's existing `map-hero` already satisfied the "Resume rail" bullet, just repointed its project fetch at `useWorkspace()`; deliberately skipped the breadcrumb-bar sub-item as redundant with A3's topbar. Phase A now 4/7. Verified with `tsc --noEmit` + `vite build` (clean) and a static screenshot of the return banner. Next: A5 (confirm/undo on destructive actions + remaining `.catch()` gaps) or A7 (nav restructure) — A6 (rebuild the forecast site-list widget) is the meatiest of what's left. | A |
| 2026-09-11 | Shipped A5: `usePendingDelete`/`UndoToastStack` on Projects/SavedLocations/CustomerData's two delete flows; script-verified `.catch()` sweep across every page found and fixed every bare `try{}finally{}` plus a real bug (CustomerData's upload-status poller could silently loop-fail forever on one transient error); wired the last two missing `EmptyState.dependency` spots (Connections, Forecast); reviewed and deliberately kept existing button vocabulary (domain-differentiated on purpose, not actually inconsistent) and ApiKeys/Connections' native `confirm()` (security semantics, not a fit for optimistic undo). Phase A now 5/7 — only A6 (rebuild the forecast site-list widget with sparklines) and A7 (nav restructure) remain. Verified with `tsc --noEmit` + `vite build` (clean) and a static screenshot of the undo-toast stack. | A |
| 2026-09-11 | Shipped A6: rebuilt "Which locations yield well" with icon chips (PPI/CapEx/payback + a fixed-size caveat slot, so row height no longer depends on which optional fields a site happens to have — the actual fix for E3, not a CSS trick), a `<MicroBar>` revenue-magnitude bar instead of a literal sparkline (no per-site time series exists in the API; fabricating a trend would have broken the honesty-labelling convention elsewhere in this codebase), and a collapsed-by-default "Show N more — need a bigger budget" group for over-budget sites. Phase A now 6/7, only A7 (nav restructure) left. Verified with `tsc --noEmit` + `vite build` (clean) and a static screenshot of the rebuilt widget. | A |
| 2026-09-11 | Shipped A7 — **Phase A now complete, 7/7**: sidenav regrouped into Workspace/Data/Account sections; Credits genuinely merged into Billing (page deleted, its UI folded in as a "Credits" section, `/credits` now redirects) rather than just visually grouped; reviewed the "Store Data" naming complaint and found the user-facing name was already consistent, the mismatch was only in internal route/API naming with no UX impact, so left it alone rather than churn URLs/backend for a cosmetic win. Verified with `tsc --noEmit` + `vite build` (clean) and static screenshots of the grouped sidenav and merged Billing page. Next: pick up Phase B (visual production polish — per-project map thumbnails, micro-viz everywhere, screen-by-screen redesign to the Forecast bar, new Project detail page) or start Phase C (Company→Project hierarchy, which the pricing model in `docs/PRICING_MODEL.md` is blocked on). | A |
| 2026-09-11 | Shipped B2 (Phase B started): `location_count`/`report_count` added to `list_projects` (real backend change, correlated subqueries, end-to-end tested via a local Flask `test_client` run against sqlite — not just a unit test) and surfaced as `<StatChip>`s on Projects/Reports/the top-bar switcher; Dashboard stat tiles get a real "+N this week" delta from data already fetched (Credits' trend deliberately skipped — would need a ledger fetch Dashboard doesn't do, not fabricated); SavedLocations' economic score gets a `<MicroBar>` using the map's own `ramp("ppi", …)` coloring. Kept the same honesty-over-fabrication discipline as A6 throughout. Verified `tsc --noEmit` + `vite build` clean, plus a real end-to-end backend test (sqlite `_auth_db.init_schema()` → seeded rows → `app.test_client().get("/api/projects")` → confirmed `location_count`/`report_count` in the JSON), and static screenshots of both UI additions. Deliberately left ApiKeys/Credits-ledger/Activity rows untouched — real B2 candidates, not done this round. Next: B1 (map thumbnails) or B3 (the big one — screen-by-screen redesign + new Project detail page). | B |
| 2026-09-11 | Shipped B1: new `<MiniMap>` client-SVG component (no server rasterisation infra existed to build on — see B1 note for why that path was rejected), replacing Dashboard's decorative hero placeholder with real per-project saved-location dots tinted by economic score, and adding a new `leading` thumbnail slot to `<DataRow>`/`<DataList>` used on Projects' rows (batched single `listLocations()` fetch, grouped client-side — no N+1). Empty-state placeholder for zero-location projects. Frontend-only change, no backend/schema touched. Verified `tsc --noEmit` + `vite build` clean and a static Playwright screenshot of populated + empty states on both placements. Project-detail placement deferred — that page doesn't exist until Phase E (E4). Next: B3 (screen-by-screen redesign + new Project detail page) or B4 (iconography pass). | B |
| 2026-09-11 | Shipped B4: new `<Pill>` component gives every status pill (6-tone vocabulary, ~25 call sites across 8 files) a tone-matched icon from one shared mapping rather than per-site picks; every remaining bare `.meta` text (dates/amounts on Activity, Billing, Projects, CustomerData, Reports) converted to an iconed `<StatChip>`; dead `.data-row .meta` CSS rule removed once its last usage was gone. Plan-tier badges deliberately left alone (identity, not status). Frontend-only. Phase B now 3/5. Verified `tsc --noEmit` + `vite build` clean and a static Playwright screenshot of all 6 pill tones plus two real row compositions. Next: B3 (the big screen-by-screen redesign + new Project detail page) or B5 (shared filter/action-bar components from Forecast/Compare). | B |
| 2026-09-12 | Shipped B5 — **Phase B now 4/5**: confirmed scope with the user first (the map's Compare popup is a separate vanilla-JS codebase, can't literally become a shared React component). New `<FilterBar>`/`<FilterSelect>` extracted from Forecast's `HotspotBubbles` filter row. New `<ActionBar>` given a real use case, not built speculatively: SavedLocations gets checkbox multi-select (via B1's `leading` slot) + a "N selected" bar with bulk status-change and bulk delete (native `confirm()`, matching A5's precedent for multi-item destructive actions). Screenshot-driven QA caught two real bugs a code-only review would have missed: the action bar's copied-from-the-map pill radius (`999px`) broke into a giant oval once content wrapped at normal page widths (fixed → `var(--radius-lg)`, matching `.map-action-bar`'s own precedent); and the global `select{width:100%}` rule blew the bulk-action select onto its own line (fixed with the same `width:auto` override pattern already used elsewhere). Also noted but deliberately left alone: the identical `select{width:100%}` issue already affects `.data-row-trailing` (e.g. SavedLocations' own per-row status dropdown stacks under "Remove") — pre-existing, out of B5's scope, a real item for whenever DataRow's trailing selects get touched next (B3, most likely). Frontend-only. Verified `tsc --noEmit` + `vite build` clean (3 iterations, the last two purely to confirm the visual fixes) and Playwright screenshots at both normal and narrow width. Next: B3 (the last Phase B item — screen-by-screen redesign + new Project detail page) or start scoping Phase C. | B |
| 2026-09-12 | Started B3 (broken into B3.1–B3.8 sub-items, Phase A-style, since "the big one" spans 9 screens). Shipped **B3.1 — new Project detail page**: `/projects/:id` composes the map hero (MiniMap+scores), inline edit, blocking-confirm delete, and four section cards (saved locations, latest forecast from Forecast's own localStorage cache, reports, connections) — the first screen to combine everything B1/B4/B5 built. Deliberately skipped E4's "activity" section after checking `_auth_db.log_activity`'s real call sites (only 2 of Activity.tsx's 7 labelled actions are actually ever logged — a project-scoped feed would be near-always-empty) and "members" (Phase D, doesn't exist). Projects list rows now navigate to the detail page; quick-edit moved to its own "Edit" button so it's not lost. `PROVIDER_LABEL` de-duplicated into a real `lib/api.ts` export (was a private copy inside ProjectWizard.tsx). Screenshot QA caught a real bug — the hero's 4-button action row wrapped mid-word at normal width — fixed by moving Edit/Delete to lightweight corner text-buttons, keeping hero-actions to its original 2 primary CTAs. Verified `tsc --noEmit` + `vite build` (clean, 2 iterations) and Playwright screenshots of the detail page and the updated Projects row. Next: B3.2 (Dashboard) or any other B3.x screen — no fixed order, pick whichever's next session. | B |
| 2026-09-12 | Shipped **B3.2 — Dashboard**: found and fixed a real bug A2 missed — the "Recent activity" card never got AsyncBoundary treatment, so a failed `listActivity` fetch (no `.catch()` at all) left it stuck on "Loading…" forever; now has real error+retry and renders through `<DataList>`/`<DataRow>`+`<StatChip>` matching Activity.tsx's own B4 pattern instead of a hand-rolled `<ul>`. Added a "View all →" link (B3.1's section-header pattern) and made all four stat tiles real links (new `.stat-tile-link` modifier, kept off the base `.stat-tile` class since Billing shares it and isn't clickable — didn't want to imply an affordance that isn't there). Deliberately left `.stat-tile` itself bare/icon-free — it's shared identically with Billing (B3.7), so a one-sided icon pass would be a new inconsistency, not a fix. Frontend-only. Verified `tsc --noEmit` + `vite build` (clean) and a Playwright screenshot of the hover state + rebuilt activity feed. Next: any B3.x screen — B3.3 (SavedLocations) is probably the smallest remaining, B3.5 (CustomerData) the biggest. | B |
| 2026-09-12 | Ran the rest of B3 in one continuous session (user asked to keep going through B3.3–B3.8). **B3.3 SavedLocations**: closed B5's flagged `select{width:100%}` wrap bug for real this time (scoped to `.data-row-trailing select`, confirmed the only page with that pattern); rows now click through to the map for that pincode; found and fixed a real latent bug in `<DataRow>` itself doing this — the `leading` slot never got `trailing`'s `stopPropagation` guard, so the new bulk-select checkbox would have navigated the row away on click. **B3.4 Reports**: kicker fix + gave the reports list (previously the only un-card-wrapped section) its own card; confirmed report titles already name their project by default, no separate link-through needed. **B3.5 CustomerData** (the biggest page): found the session's most consequential bug — the mapping-preview table used `border-bottom:1px solid var(--line)`, an undefined CSS custom property, so every row border silently rendered as `none`; fixed to `var(--border)`. Plus 5 kicker fixes and 2 missing card-wraps (store list, past uploads). **B3.6 Connections**: the exact same `var(--line)` bug turned out to be copy-pasted into this page's `thStyle`/`tdStyle` too (2 more tables); fixed, then grepped the whole `workspace/src` tree to confirm zero remaining instances anywhere. Rest of the page already fine as-is. **B3.7 Billing**: closed the `.stat-tile` icon question deferred from B3.2 (decided: stays bare everywhere, both pages that use it now consistently icon-free) and card-wrapped its own two orphaned sections (credit history, invoices), same pattern as B3.4/B3.5. **B3.8 Activity + ApiKeys — B3 complete, 8/8**: ApiKeys was already at the bar, no changes; Activity had a real bug — its `ACTION_LABELS` was a second, drifted copy of Dashboard's (missing 4 of 7 action types), so the full activity history page would show raw backend strings for entries Dashboard's own mini-feed already humanized; deduplicated into `lib/activity.ts`. **Phase B now complete, 5/5.** Established two durable rules along the way: single-purpose list pages stay bare (no card), multi-section pages get one card+kicker per section. Verified every step with `tsc --noEmit` + `vite build` (all clean) and Playwright screenshots for every visual change. Next: Phase C (Company→Project hierarchy, blocks the pricing model) or Phase F/G/H (can start once C lands) — Phase B was the last "visible wins, no schema risk" phase; everything from here needs the org/company migration. | B |
| 2026-09-12 | Started Phase C. Before writing code, checked what "C1: decide the model" actually still needed deciding — most of it was already locked by `docs/PRICING_MODEL.md` rev 6, and found a real gap: Phase C as originally scoped never moved credits/plan to the company level even though the pricing model's own P1 requires it. Raised both findings with the user directly rather than assuming; they chose to expand Phase C's scope to include credits/plan, and to build it staged (additive-first, migration as its own later step). Shipped **C2**: `blueprints/organizations.py` (full CRUD + role-gated membership) + `_auth_db.py` functions, plus additive-only schema (`organizations.plan`/`website_url`, nullable `org_id` on 4 tables) — nothing existing changes behavior, every new column stays NULL/unread until a later cutover. Verified with a full local Flask `test_client` pass against real sqlite (19 assertions: CRUD, membership, and every permission edge including last-owner protection) rather than just unit-testing the DB functions in isolation. **C3 (the actual data migration) deliberately not started this session** — it's the step that touches production data and the payment surface, gets its own explicit go-ahead first. Next: C3 (migration) when ready to proceed, or pause here since C2 alone is a safe, real, shippable checkpoint. | C |
| 2026-09-12 | Built + thoroughly tested **C3** (the backfill migration), but deliberately stopped short of running it against production — that's the one explicit go/no-go this session held back, per the plan above. `_auth_db.backfill_organizations()` + `db/backfill_organizations.py`, matching the codebase's existing `migrate_csv_to_db.py` script convention exactly. Verified against real sqlite across multiple runs and scenarios (multi-project user, zero-project user, no-name user, website_url carry-up, full idempotency on a clean re-run, new activity created between runs correctly picked up, and oauth_connections/customer_uploads/customer_locations all separately confirmed) — no gaps found. Still effectively additive: nothing reads org_id yet, so running it against prod would change zero live behavior, only prepare data. Next: explicit go-ahead from the user to run it against production, then C4 (company switcher UI, now has real backfilled data to scope against) and C5. | C |
| 2026-09-12 | Got explicit go-ahead and **ran C3 for real against production — C1-C3 all done.** Committed the whole session's work (Phase B complete + Phase C's C1-C3) as `959d52b`, rebased cleanly past 2 unrelated bot enrichment-sync commits that had landed on `main` meanwhile, pushed, confirmed the `Deploy PaisaMap` Actions run went green, confirmed `/api/organizations` live over HTTPS. Took a read-only SSH snapshot before touching anything (1 user, 4 projects, 0 orgs — small, low-risk). Ran the backfill for real: 1 org created ("raghotham harish's Workspace", plan `pro` carried over, `website_url` carried from a project), 4 projects + 6 oauth connections + 12 credit-ledger rows stamped with its `org_id`. Verified the actual resulting rows over SSH (not just the script's own summary counts), re-ran it a second time against prod to reconfirm idempotency holds for real — zero writes — and did a final smoke check confirming `/` and the existing unauth'd API responses are completely unchanged. Every company/org table now has real production data, ready for C4 to build against. Next: **C4** (company switcher + "Company settings" screen — first real frontend surface for any of this) or **C5** (re-point Forecast/expansion/reports so store data + connections share across a company's projects). | C |
| 2026-09-12 | Shipped **C4**: `<CompanySwitcher>` + `/company` Company Settings page (rename/website, member management, create-additional-company, delete-company), `WorkspaceProvider` extended with org state alongside its existing project state. Closed a real gap found while building this: C3's backfill only covered *existing* accounts, so every signup after it would have started with zero companies — added the same default-org creation to the real signup route (`blueprints/auth.py`), refactored into one shared `_auth_db` function both call rather than duplicated. New projects now get tagged with the active company at creation (`create_project` gained a permission-checked, always-falls-back-never-errors `org_id` param). Deliberately did NOT add company-based filtering to the project list itself — every real account has exactly one company today, so there's no second company to verify that filter against; shipping unverifiable filtering now was judged worse than shipping the real, testable pieces and adding the filter once there's something real to test it with. Verification matched the sensitivity of what was touched: the signup path (most sensitive thing touched this phase) got a full end-to-end Flask `test_client` pass against the real `/api/auth/google` route with Google's own token verification patched out (not skipped) — not just a unit-level check on the two functions it calls — plus `create_project`'s org_id fallback logic tested through the real HTTP body (valid org, no org, someone else's org, a malformed string), and a Playwright screenshot of the full topbar + settings page. `tsc --noEmit` + `vite build` both clean. Next: **C5** (re-point Forecast/expansion/reports so store data + connections actually share across a company's projects — the natural pairing for whenever project-list company-filtering above gets built too, since both need a second real company to verify against). | C |
| 2026-09-12 | Started **C5**, but the real production data changed the plan before any code got written: checking the account's actual connections first (not just assuming the roadmap's design intent held) found 3 genuinely distinct GA4 properties / Search Console sites across the company's 4 projects — different businesses/clients, not one business viewed from different angles, the opposite of C5's original "share connections across the company" premise. Went back to the user with this finding rather than building the wrong thing; they confirmed store data should share (their call, despite the same distinct-businesses pattern) but connections should stay per-project *with the ability to pick from the company's pool* — support multiple connections per company, not collapse to one. Shipped both: store data now org-scoped at read (upload once, visible to every project in the company) and creation-time-tagged; connections got genuinely new infrastructure — a `project_connection_selections` table decoupling "who created a connection" from "who's using it," full pool/switch/disconnect semantics (a shared connection survives one project unlinking; a sole-use one really deletes + revokes at Google), and a `Connections.tsx` picker UI. One choke-point design win: every GA4/GSC data-reading function already funneled through `_get_valid_access_token`, so fixing that one function made report/ecommerce/location-tags/PDF-digital-baseline all company-aware for free. Verification matched the real stakes (3 live production OAuth connections with real encrypted tokens): a local test replicating the exact prod shape (1 org, 3 projects, each with its own connection) covering every real edge case — default resolution, pool listing, switching, cross-org rejection, both disconnect paths, reconnect, backfill idempotency, store-data cross-project sharing, cross-org isolation — through both direct function calls and real Flask HTTP routes, plus a full C2-C4 regression pass since this touched shared `_auth_db.py` code extensively. A screenshot caught one more real bug (the recurring `select{width:100%}` issue, same class as B3.3/B5, hit the two new pickers) — fixed. `tsc --noEmit` + `vite build` clean. **Not yet done**: `backfill_connection_selections()` still needs to run against production for the 3 real existing connections — same "build + test locally first, explicit go-ahead before touching prod" discipline as C3. Phase C is now C1-C5 built; only that one prod migration step remains before it's fully closed out. | C |
| 2026-09-12 | Deployed C5's connections change and ran its production backfill — **Phase C fully complete, C1-C5 all live.** Committed + pushed (`534e415`), confirmed the deploy went green. Before running the migration, a read-only SSH check found a real live regression window: the new code (already deployed) resolves connections only through `project_connection_selections`, which didn't exist as data yet, so all 3 of the account's real GA4/Search Console connections were resolving to "not connected" until the backfill ran — confirmed this, then ran the backfill immediately rather than leaving it live. Added `db/backfill_connection_selections.py` (same script convention as C3's), committed, deployed, ran it against prod: all 6 connection rows (GA4 + Search Console × 3 projects) got their explicit selection in one pass. Verified every one resolves to the exact same connection id and external_ref (GA4 property / Search Console site) it had before any of this started — a byte-for-byte match against the very first read-only snapshot taken at the start of C5, not just "no errors." Re-ran the backfill a second time against prod to confirm idempotency for real (zero writes), then a final live smoke check confirmed the site and existing APIs are unaffected. **Phase C (Company → Project hierarchy) is done**: organizations exist for real, every account gets one at signup, there's a real UI to manage them, and store data + connections both now work at the right scope for how this product actually gets used. Next: whichever of Phase D (roles/permissions — now has real membership to build against), F/G/H (demand intelligence, signal synthesis, security hardening — all "can start after C"), or Phase E (project-level actions) the user wants next. | C |
| 2026-09-12 | Started Phase D. Checked reality before picking a bullet: D1 (email invites) needs a transactional email provider that flatly doesn't exist in this codebase — no SMTP/SendGrid/SES anywhere. D3 (member-management UI) turned out to already be mostly shipped, as a side effect of C4. That left D2 (permission checks) as the one real gap, and checking it confirmed why: zero endpoints checked org role — a "member" added via Company Settings could manage the company page but couldn't touch a single actual project. Locked the read/write/delete policy with the user first (member: read+write, no delete; admin: +delete; owner: +delete-company) before writing code. **Shipped D2**: routed the permission check through `get_project` (the keystone almost every other function's ownership check already calls) rather than a separate middleware layer — one change propagates everywhere. `list_projects`/`list_saved_locations`/`list_reports` now return everything across every company a user belongs to; every delete function requires owner/admin specifically. Found and fixed two real, unrelated bugs while touching this code: a would-be 0-count bug in the upload quality-report stats once uploads became teammate-visible (C5), and a genuine pre-existing production bug where `/api/reports/<id>/share`/`.../unshare` had always returned `{"report": null}` (the function never returned its result). Verified with the most rigorous suite yet, matching what's at stake: a real multi-member scenario (owner + admin + member + an unrelated outsider org) across every resource type, confirming blocked deletes actually delete nothing, plus a full single-owner regression proving today's real production shape is completely unaffected. `tsc`/`vite build` clean. Deliberately deferred: hiding Delete buttons for a plain member in the UI — no second real member exists yet to verify that against; the backend enforces the policy regardless. **Deployed and verified live same session** (`79f8f42`): confirmed the deploy went green, server on the exact commit, then a read-only check against the real production account confirmed all 4 real projects still resolve with `role=owner`, the same location/report counts as always, and all 3 real GA4 connections still resolve to the exact same connection ids/external_refs as before — D2 is a byte-for-byte no-op for the account's existing behavior, exactly as the local regression predicted, while now correctly enabling company-wide access for whenever a real second member exists. Final live smoke check clean. **Phase D now: D2 and D3 done, D1/D4 remain (both independent of each other and of D2/D3).** | D |
| 2026-09-12 | Shipped **D4** — data-access audit log. Checked what actually needed logging before writing code: `/api/export` (the literal "export" in D4's own title) had never been logged, at all, since it predates the auth stack; `download_report`/`view_shared_report` in `reports.py` were the same gap. Added a `log_data_access()` helper in `_session.py` (same X-API-Key→session→anonymous identity resolution as `get_effective_plan()`) and wired it into all three, writing to the existing `activity_log` table — no schema change. New `GET /api/organizations/<id>/audit-log` (owner/admin only) filters to a deliberate `DATA_ACCESS_ACTIONS` subset (export/report-generate/download/share-view/location-score/location-compare/customer-upload) rather than the full personal feed — login/purchase stay off it. New "Data-access audit log" card on Company Settings, gated the same way the Members card already is. Accepted limitation, consistent with C4/C5's own discipline: a fully anonymous export/share-view has no `user_id` to attribute to any org, so it won't show in that org's audit log — not worth building a report→project→org attribution join with only one real org to test it against. Verified with a real Flask `test_client` HTTP pass: member's real `/api/export` call attributes correctly, an anonymous call is correctly invisible to the audit log, outsider and plain-member both get a real 403, `login` correctly excluded, single-owner prod shape returns cleanly with zero entries (no regression). Also ran a real local-browser Playwright screenshot (own Flask+Vite dev servers, minted session cookie, seeded sqlite data) confirming the new card renders correctly with no console errors. `tsc --noEmit` + `vite build` clean. **Deployed and verified live same session** (`4ed0c93`/`5896e47`): rebased past a same-day cron data-sync commit, pushed, confirmed the deploy went green, then confirmed the new endpoint is real in production (401 for an unauthenticated request, not 404) and `/api/export` is unaffected (still 200). A read-only SSH check calling `list_org_audit_log` directly against the real production database showed real entries — the actual account's own recent `location_score` history — correctly attributed to the real user, and a non-member id correctly got `None` (the 403 path). Phase D now: D2/D3/D4 done, only D1 (blocked on missing email infra) remains. | D |
| 2026-09-12 | Shipped **D1** — email invites, unblocking the "needs a vendor decision" note above by not actually needing one yet: SES-backed `_email.py` degrades gracefully (same shape as `blueprints/billing.py`'s Razorpay `_client()`) when unconfigured, returning `accept_url` directly in the API response as a fallback. New `org_invites` table + full lifecycle in `_auth_db.py`/`blueprints/organizations.py`, `InviteAccept.tsx` working pre- and post-sign-in. Verified with a local Flask `test_client` pass, then deployed (`a6512e6`), schema-migrated on prod, and — since this both writes to the live database and needed a session-cookie mint, both gated by Claude Code's auto-mode classifier by default — did a real prod-DB smoke test (create→list→public-lookup→revoke) after getting explicit user approval. **Phase D now fully complete, D1-D4 all shipped and live.** | D |
| 2026-09-12 | Started **Phase E** — checked what was already true before writing anything (B3.1 had already shipped much of the Project detail hub, Compare already had "Save report") rather than assuming all 4 bullets were greenfield. Shipped all of **E1-E4** in one pass: E1 added the one real gap, "Save report" on Forecast (Compare already had it); E2 added a public read-only project share link reusing the report `share_token` pattern exactly, after realizing "share with company members" was already satisfied structurally by D1+D2; E3 added duplicate + archive (additive `archived_at`/`share_token` columns) and rewired the detail page's delete through A5's real undo pattern instead of `window.confirm`; E4 added the last missing "members with access" section now that Phase D's roles are real. Found and fixed a real bug surfaced by E3: archiving a project made its own detail page 404 (`WorkspaceProvider`'s shared list excludes archived projects by design, but `ProjectDetail.tsx` had no fallback), fixed with a new direct `api.getProject` fetch. Verified with a full local Flask `test_client`/direct-function pass against a **real multi-member scenario** (owner/admin/member/outsider, not just today's single-owner shape) covering every permission boundary, plus a full local-browser Playwright pass (seeded sqlite, 3 real members, minted cookie) screenshotting every new surface including the archived-detail-page bug fix and the delete-undo toast actually undoing. `tsc --noEmit` + `vite build` clean. One deliberate gap: the Forecast "Save report" button itself wasn't pixel-verified live (needs a full national candidate-pincode dataset this local QA sqlite didn't have) — code-reviewed against Compare's identical, already-shipped pattern instead. **Phase E complete, 4/4** — not yet deployed this session. | E |
